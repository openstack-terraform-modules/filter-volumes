locals {
    deployment = var.get-volumes.deployment
    inventory_file_path = var.get-volumes.inventory_file_path
    cluster_prefix = var.cluster_prefix
    group_prefix = var.group_prefix
    volume_name = var.volume_name
}